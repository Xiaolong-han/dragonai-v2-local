## 修复方案

**修改文件**：`frontend/src/utils/markdown.ts`

**修改内容**：在 `renderMarkdown` 函数中添加后处理逻辑，自动检测并转换纯文本图片 URL 为 `<img>` 标签。

**具体实现**：
1. 检测以 `/api/v1/files/serve/` 开头的路径
2. 检测常见图片扩展名（.png, .jpg, .jpeg, .gif, .webp 等）
3. 将匹配的 URL 自动包装成带下载按钮的图片容器

这样无论 AI 返回的是 Markdown 图片语法还是纯文本 URL，都能正确显示图片。